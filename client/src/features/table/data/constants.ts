// React Query Keys
export const KEYS = {
  ROWS: ["rows"],
};

// Dropdown Options
export const STATUS_OPTIONS = ["Active", "Pending", "Blocked", "Archived"];
export const PRIORITY_OPTIONS = ["High", "Medium", "Low"];
